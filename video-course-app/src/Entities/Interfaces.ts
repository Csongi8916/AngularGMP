export interface User {
  id: string;
  firstName: string;
  lastName: string;
}

export interface Course {
  id: number;
  name: string;
  length: number;
  date: Date;
  description: string;
  topRated: boolean;
}

