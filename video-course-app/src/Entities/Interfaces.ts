export interface User {
  id: string;
  firstName: string;
  lastName: string;
}

export interface Course {
  id: number;
  title: string;
  duration: string;
  date: string;
  description: string;
  topRated: boolean;
}

